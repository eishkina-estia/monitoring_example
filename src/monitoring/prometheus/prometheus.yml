global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "wine_api"
    static_configs:
#      - targets: ["host.docker.internal:8000"]
#      - targets: ["localhost:8000"]
      - targets: ["api:8000"]